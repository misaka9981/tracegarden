# 20: Prove the complete local MVP core loop

**What to build:** A maintainer can run one authoritative local acceptance workflow demonstrating that Tracegarden preserves Kubernetes-derived facts and structured human context without overstating causality. The workflow combines deterministic Kubernetes input, real disposable PostgreSQL, bilingual browser behavior, non-root images, and offline deployment validation while preserving the boundary around live infrastructure.

**Blocked by:** 18: Declare isolated Preview Environments and pull-based promotion; 19: Provide a disabled encrypted backup and restore path.

**Status:** ready-for-agent

- [ ] One Playwright scenario covers admitted Member login, a deterministic Observation, live Timeline delivery, Experiment creation, Correlation Suggestion review, Confirmed Link creation, and reconstruction after returning to persisted history.
- [ ] The scenario also proves that an unconfirmed Correlation Suggestion is never presented as cause and that rejection remains distinct from confirmation.
- [ ] The acceptance workflow runs against a real disposable PostgreSQL database and does not depend on Google, Cloudflare, GitHub, R2, Argo CD, or a live Kubernetes Cluster.
- [ ] Simplified Chinese default behavior and English switching are exercised across the complete core loop.
- [ ] Deterministic failure-path suites, repository integrations, authorization tests, retention tests, telemetry redaction tests, and Recent Log Window bounds all pass as part of the same reproducible verification entry point.
- [ ] Production web and collector images build and pass non-root smoke tests.
- [ ] Deployment, Preview Environment, promotion, and disabled backup declarations render and validate offline.
- [ ] The final evidence explicitly lists live Kubernetes compatibility, OAuth callbacks, Cloudflare Access, Argo CD reconciliation, remote artifact publication, backup upload, restore rehearsal, and production promotion as unverified.
